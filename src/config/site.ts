/**
 * Site configuration.
 *
 * To switch to GitHub Pages: set `USE_CUSTOM_DOMAIN` to `false`.
 */

/** Whether the site is deployed to a custom domain (true) or GitHub Pages (false). */
export const USE_CUSTOM_DOMAIN = true;

/** The custom domain URL (used when `USE_CUSTOM_DOMAIN` is true). */
export const CUSTOM_DOMAIN_URL = "https://kolloxfm.com";

/** The GitHub Pages URL (used when `USE_CUSTOM_DOMAIN` is false). */
export const GITHUB_PAGES_URL = "https://nathan-abela.github.io/kollox-fm";

/** The repository name, used for GitHub Pages base path. */
export const REPO_NAME = "kollox-fm";
